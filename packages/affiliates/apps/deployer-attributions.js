module.exports = config => {
  const {
    // emps which we want to give rewards to
    empContracts=[],
    // affiliate addresses to specifically whitelist
    whitelist=[],
    // tokens to get prices for
    tokenAddresses=[],
    // collateral to get prices for
    collateralAddresses=[],
    empCreator,
    startTime,
    endTime,
    totalAwarded,
  } = config


}
